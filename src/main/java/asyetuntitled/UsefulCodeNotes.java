package asyetuntitled;

public class UsefulCodeNotes
{
    
    /*
     * This is just a collection of code snippets that are cool but not currently used in the mod.
     * 
     * These two display a message as a chat or as a gui message (like a bed)
     * player.displayClientMessage(new TranslatableComponent("asyetuntitled.charlie.attack.message" + (1+player.level.random.nextInt(6))), true);
     * player.sendMessage(new TranslatableComponent("asyetuntitled.charlie.attack.message" + (1+player.level.random.nextInt(6))), Util.NIL_UUID);
     *
     */

}
